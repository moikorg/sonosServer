version: "2"

services:
  sonosserver:
    build: .
    command: python3 sonosServer.py 
    ports:
      - "5000:5000"
    volumes:
      - ./code:/code
    restart: always
    network_mode: "host"

